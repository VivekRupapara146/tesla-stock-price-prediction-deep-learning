# ─────────────────────────────────────────────────────────────────────────────
# Tesla Stock Price Prediction — Python Dependencies
#
# IMPORTANT — TensorFlow version:
# Pin tensorflow to the SAME version used when training your model.
# Mismatch causes: "Unrecognized keyword arguments: [batch_shape, optional]"
#
#   TF <= 2.15  →  uses Keras 2 (batch_shape in InputLayer config)
#   TF >= 2.16  →  uses Keras 3 (different serialization format)
#
# Check your training version with:
#   python -c "import tensorflow as tf; print(tf.__version__)"
# Then set that exact version below.
# ─────────────────────────────────────────────────────────────────────────────

# Core ML / Deep Learning
tensorflow==2.16.1      # ← change this to match your training environment
scikit-learn==1.4.2
numpy==1.26.4

# Data Handling
pandas==2.2.2

# Web App
streamlit==1.35.0

# Visualization
plotly==5.22.0

# Serialization (scaler loading)
joblib==1.4.2